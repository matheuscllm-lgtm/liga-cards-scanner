# O scanner usa apenas a stdlib (urllib) para HTTP + openpyxl para o XLSX.
# requests/beautifulsoup4/lxml foram removidos por nao serem usados (o modo
# de scraping HTTP da Liga e um stub; se for implementado, readiciona-los aqui).
openpyxl>=3.1.0
